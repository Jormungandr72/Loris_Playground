# Stores a collection of cards that represent a player's deck
class deck:
    """
    Constructs a Deck object
    
    @param cards: an array of cards that are in the deck
    """
    def __init__(self, cards):
        self.cards = cards